name: "🔔 Replit Wake-Up Call"
on:
  schedule:
    - cron: "*/4 * * * *"  # Каждые 4 минуты
  workflow_dispatch:  # Ручной запуск

jobs:
  ping-server:
    runs-on: ubuntu-latest
    steps:
      - name: 📞 Звонок Replit серверу
        env:
          SERVER_URL: ${{ secrets.REPLIT_SERVER_URL }}
        run: |
          if [ -z "$SERVER_URL" ]; then
            echo "❌ REPLIT_SERVER_URL не установлен в Secrets!"
            echo "Добавь в GitHub Secrets:"
            echo "REPLIT_SERVER_URL=https://твой-проект.твой-юзер.repl.co"
            exit 1
          fi
          
          echo "Отправляю запрос на: $SERVER_URL/wake-up"
          
          RESPONSE=$(curl -s -w "\n%{http_code}" "${SERVER_URL}/wake-up")
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')
          
          echo "Статус код: $HTTP_CODE"
          echo "Ответ: $BODY"
          
          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Сервер успешно разбужен!"
            echo "Время: $(date '+%Y-%m-%d %H:%M:%S')"
          else
            echo "❌ Ошибка: сервер не ответил"
            exit 1
          fi
